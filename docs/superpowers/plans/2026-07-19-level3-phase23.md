# Level 3 Phase-2/3 收尾执行计划（Task #10）

- **日期**: 2026-07-19
- **Spec**: `docs/superpowers/specs/2026-07-19-level3-phase23-design.md`
- **落地方式**: 采集器为主（用户确认）；5 服务全补（Athena/RAM/SecretsManager/CloudFront/OpenSearch）
- **纪律**: Spec+Plan-before-implement → Fan-out 子 Agent → Token Efficiency Monitor → §12 split (codegraph/grep) → GCL reflect-until-clean → commit

## 步骤与验证点

### 0. 准备（主 Agent）
- [ ] 确认 `_inference.py` 当前 rule 清单（已 grep 复核，见 spec §1）
- [ ] 确认 `make_incident` 签名、`registry.py` 合并模式（已读）
- [ ] `codegraph sync .`（§12：代码改动前建立 code graph 基线）

### 1. Fan-out：5 个并行子 Agent（每个独立 signals key）
每个子 Agent 拿到 spec §3 对应行的片段 + 数据可用性约束（"仅用 describe / CloudWatch metric，不自动写操作"）。

| Agent | 服务 | 交付 |
|-------|------|------|
| A | Athena | `collectors/analytics.py`(新) + `_inference.py` 内联段 + `registry.py` 登记 |
| B | RAM | `collectors/governance.py` 增补 + `_inference.py` + `registry.py` |
| C | SecretsManager | `collectors/governance.py` 增补 + `_inference.py` + `registry.py` |
| D | CloudFront | `collectors/edge.py` 增补（CF-ORIGIN-02/CF-CACHE-01 + signals） + `_inference.py` + `registry.py` |
| E | OpenSearch | `collectors/search.py`(新) + `_inference.py` + `registry.py` + `_shared.py` PRODUCTS(可选) |

> **冲突规避**：所有子 Agent 改 `_inference.py` 时，各自把内联段写在 `apply_chain_inference()` 的**不同位置/独立 signals key**（Athena/RAM/SecretsManager/CloudFront/OpenSearch 互不相同），主 Agent 最后串行拼接到函数末尾，避免同一行并发编辑。

每个 Agent 自测：`pytest tests/test_inference_phase23.py`（合成 signals 命中/不命中）。

### 2. 主 Agent 串行合并（避免并发冲突）
- [ ] 把 5 段内联规则拼接到 `apply_chain_inference()` 末尾
- [ ] 把 5 个采集器登记进 `registry.py` 的 `collectors` 列表（参考 EKS 的 `signals` 合并分支）
- [ ] `ruff check` 零 error

### 3. Token Efficiency Monitor（每服务一段）
- [ ] 对 5 段实现各跑一次 Token Efficiency Monitor 子 Agent，判 `OPTIMAL`/`REFACTOR-NOW`/`ACCEPT-SUBOPTIMAL`
- [ ] `REFACTOR-NOW` 项就地精简

### 4. §12 split 验证
- [ ] 代码改动：`codegraph explore` 确认新增 `aws-<svc>-ops` 引用真实存在（路由表 Phase-1 已存在）
- [ ] Markdown 改动（addendum）：`grep` 修正自相矛盾表格为 5 条 "✅ implemented"

### 5. 测试门禁
- [ ] `pytest aws-aiops-cruise/tests/` 全绿（含 `test_inference_phase1.py` / `test_shared.py` 零 regression）
- [ ] grep 确认无 `modify-/delete-/terminate-` 自动调用

### 6. GCL reflect-until-clean
- [ ] 每服务 Generator + ≥2 Critic，零问题后通过

### 7. 收尾
- [ ] 修 `references/inference-rules-addendum.md` 矛盾表格
- [ ] 更新/删除 `docs/RESUME-2026-07-19.md`
- [ ] 统一 commit（每服务独立 commit 更佳，遵循 §16.1）
- [ ] 输出 `[OK] aws-aiops-cruise — Task #10 Phase-2/3 closed`
