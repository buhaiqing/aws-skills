# Agentic Maturity — Progress Note (2026-07-31)

> **Date**: 2026-07-31  
> **Scope**: Bootstrap evidence vs ADR-0001 Milestone 1 exit  
> **SoT**: [`docs/agentic-maturity-model.md`](../../agentic-maturity-model.md) + this note + [`docs/adr/0001-l4-production-evidence-loop.md`](../../adr/0001-l4-production-evidence-loop.md)

---

## TL;DR

| Level | 2026-07-26 audit | 2026-07-31 (current) | Δ | Notes |
|---|---|---|---|---|
| **L1** Foundational | 95% | **100%** | +5 | Charter C2 形式合规已闭环 |
| **L2** Operational | 92% → 100% (P0) | **100%** | 0 | composite_lint + CI hooks |
| **L3** Orchestration | 100% | **100%** | 0 | 37/37 GCL, cross-refs 0 missing |
| **L4** Adaptive | 88% → 99% (scripts) | **99% bootstrap / M1 partial** | — | 评测覆盖面↑；M1 深度门未过 |

**Headline**: 静态治理与 golden bootstrap 已到位；稳定 L4 证据闭环（ADR-0001 M1）仍在进行中。

---

## Evidence (2026-07-31)

- **TE Gate**: `python3 scripts/te_gate.py --all --strict` — 37/37 PASS（P0-B backlog closed 2026-07-28）。
- **Golden eval**: 45 × `golden-scenarios.yaml`，274 场景，`golden_eval` 45/45 PASS；每文件 ≥5。
- **Confirmation Strings**: 全仓 Operation/token 规范表统一 — commit `f215a6a` (`refactor(skills): unify Confirmation Strings to Operation/token tables`)。
- **Pre-commit**: hard gate 含 pytest / te_gate / composite_lint；**CodeGraph sync 为 soft**（`codegraph sync . || true`，不阻断 commit）。
- **High-risk depth (Wave 1 complete)**: EC2 14、S3 13、IAM 11、RDS 10、KMS 10，合计 **58** 场景；`golden_eval --all-high-risk` 58/58 PASS。

---

## Historical vs current SoT

| Document | Role |
|---|---|
| [`maturity-2026-07-26.md`](maturity-2026-07-26.md) | **Historical** — 首次“实现 vs 强制生效”诚实审计 |
| [`docs/agentic-maturity-model.md`](../../agentic-maturity-model.md) | **Primary SoT** — L1–L4 能力库存 + 状态图例 |
| [`docs/adr/0001-l4-production-evidence-loop.md`](../../adr/0001-l4-production-evidence-loop.md) | **Delivery gate** — 稳定 L4 的四条主线 + M1–M4 exit criteria |
| **This note** | **Point-in-time delta** — bootstrap 完成项 vs M1 仍开放项 |

Do not cite `maturity-2026-07-26.md` percentages as current without cross-checking the model doc and ADR progress.

---

## Next (ADR-0001 after M1 Wave 1–2)

**Wave 1–2 complete**: 58 high-risk scenarios；`evals/` schema；`--all-high-risk`；mutation CI + `BLOCKED`/`COMPENSATED` schema；dashboard snapshot 已生成。

**Still open**: 30-day dashboard full-window baseline（warm-up 中）；其后才是 ADR M2 Shadow Execution。满窗前不扩大 AUTO_HEAL。

---

## Verification commands

```bash
python3 scripts/te_gate.py --all --strict
python3 -c "from pathlib import Path; import yaml; fs=list(Path('.').rglob('golden-scenarios.yaml')); print(len(fs), sum(len(yaml.safe_load(p.read_text())['scenarios']) for p in fs))"
git config core.hooksPath   # expect scripts/hooks when installed
```
