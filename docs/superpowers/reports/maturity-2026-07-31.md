# Agentic Maturity — Progress Note (2026-07-31)

> **Date**: 2026-07-31  
> **Scope**: ADR-0001 M1–M4 engineering vs remaining L4 gates  
> **SoT**: [`docs/agentic-maturity-model.md`](../../agentic-maturity-model.md) + this note + [`docs/adr/0001-l4-production-evidence-loop.md`](../../adr/0001-l4-production-evidence-loop.md)

---

## TL;DR

| Level | 2026-07-26 audit | 2026-07-31 (current) | Δ | Notes |
|---|---|---|---|---|
| **L1** Foundational | 95% | **100%** | +5 | Charter C2 形式合规已闭环 |
| **L2** Operational | 92% → 100% (P0) | **100%** | 0 | composite_lint + CI hooks |
| **L3** Orchestration | 100% | **100%** | 0 | 37/37 GCL, cross-refs 0 missing |
| **L4** Adaptive | 88% → 99% (scripts) | **99% + M1–M4 工程 ✅** | — | M4 Governed Learning 已交付；满窗基线仍 warm-up |

**Headline**: ADR-0001 **M1–M4 工程交付完成**（58 high-risk + shadow 27/27 + 三链 9/9 + governed-learning dup 8% / auto_promo 0%）；仅剩 30 天 dashboard 满窗基线（阻塞 AUTO_HEAL）。

---

## Evidence (2026-07-31)

- **TE Gate**: `python3 scripts/te_gate.py --all --strict` — 37/37 PASS。
- **Golden eval**: 45 × `golden-scenarios.yaml`，274 场景，`golden_eval` 45/45 PASS。
- **High-risk + M1**: **58** scenarios；`--all-high-risk` 58/58；mutation CI + outcome 五态。
- **M2 Shadow**: `execution_plan` / `shadow_exec` / plan-bound proxy；destructive **27/27**。
- **M3 Transactional**: `execution_dag` / `compensation_runner` / `chain_fixtures`；**9/9** chain×mode；compensation recovery **100%**；non_compensable → **MANUAL**。
- **M4 Governed Learning**: `governed_learning.py` harvest/evaluate/approve；fixture **dup 8%**；**auto_promotion_rate=0%**；CI 已接入。
- **Pre-commit**: CodeGraph sync 在 binary 存在时 hard-fail；`links_lint` SR-4 已接入。

---

## Historical vs current SoT

| Document | Role |
|---|---|
| [`maturity-2026-07-26.md`](maturity-2026-07-26.md) | **Historical** — 首次“实现 vs 强制生效”诚实审计 |
| [`docs/agentic-maturity-model.md`](../../agentic-maturity-model.md) | **Primary SoT** — L1–L4 能力库存 + 状态图例 |
| [`docs/adr/0001-l4-production-evidence-loop.md`](../../adr/0001-l4-production-evidence-loop.md) | **Delivery gate** — M1–M4 exit criteria + Progress |
| **This note** | **Point-in-time delta** — M1–M4 完成项 vs 仍开放项 |

---

## Next (ADR-0001 after M4)

**M1–M4 complete**: evidence foundation + shadow gate + transactional DAG/compensation + governed learning。

**Still open**: 30-day dashboard full-window baseline（warm-up；阻塞 AUTO_HEAL 扩大）；自动 skill 生成闭环。

**Next focus**: **M1 满窗 telemetry 关单**（不自动扩大 AUTO_HEAL）；可选 live `--audit-dir` harvest。

---

## Verification commands

```bash
python3 scripts/te_gate.py --all --strict
python3 scripts/shadow_coverage.py check --all-high-risk --shadow-dir /tmp/shadow-m2
python3 scripts/chain_fixtures.py run --all --shadow-root /tmp/m3-chains
python3 scripts/governed_learning.py harvest --fixtures --out /tmp/gl-queue.json
python3 scripts/governed_learning.py report --queue /tmp/gl-queue.json
python3 scripts/golden_eval.py run --all-high-risk --out audit-results/golden/high-risk.json
```
