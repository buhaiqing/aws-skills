# Resume State — aws-skills session (2026-07-19)

> **Purpose**: This file lets a FRESH session continue seamlessly. If the user says
> "请帮我继续往下完成所有的任务" (or similar), read this file first, then
> continue per the standing rules below. Delete this file once all pending work is closed.

## 1. Session standing rules (MANDATORY, strictly enforced in every action)

These were added this session as hard user constraints. A fresh session MUST obey them:

1. **Fan-out Subagents (as much as feasible) — MANDATORY** (AGENTS.md §Operational Guidelines):
   Any task splittable into ≥2 independent, non-shared-state subtasks MUST be
   fanned out to parallel subagents in ONE message. Main Agent only orchestrates + synthesizes.

2. **Token Efficiency Monitor (MANDATORY GATE)** (AGENTS.md §Operational Guidelines):
   EVERY task must pass an independent Token Efficiency Monitor subagent before being
   declared done. Monitor verdicts: `OPTIMAL` / `REFACTOR-NOW` / `ACCEPT-SUBOPTIMAL`.
   Long-lived docs (AGENTS.md / README / SKILL.md) default to clarity > token savings.

3. **§12 Mandatory Split Gate** (AGENTS.md §12, data-driven from A/B experiment):
   - Non-code docs (`.md` frontmatter/delegate/routing, `references/*.md`, `AGENTS.md`, `*.yaml`) → **Grep / `git grep`** (CodeGraph is blind to non-code).
   - Code files (any tree-sitter language: `.py`/`.ts`/`.go`/`.rs`/`.js`/`.java`) impact radius → **`codegraph sync .` + `codegraph explore`**.
   - Any query → construct glob/regex correctly (a bad query is more insidious than a bad tool).

4. **§13 CADL** (Compound-Asset Distillation Loop): every substantial task completes
   extract→landing-decision→write→gate. Pitfalls → `docs/failure-patterns.md`;
   general patterns → AGENTS.md.

5. **Spec + Plan before implement** (dogfood discipline): non-trivial work gets a spec+plan
   first; the rollout commit references them.

## 2. What is DONE this session (committed to `main`)

| Commit | What |
|---|---|
| `cd0fe4c` | Charter C7 enforces base/composite contract |
| `79fc2c6` | Document composite/copilot skill layering + contract |
| `1993391` | Distill compound-engineering lesson into CADL section |
| `4427043` | Sink dogfooding-discipline CADL lesson (finsecops review) |
| `d1f0daa` | CodeGraph applicability boundary (code vs markdown split) |
| `4d77f57` | Enforce codegraph/grep split as binding gate |
| `99adbde` | Record CodeGraph A/B experiment spec+plan+execution report |
| `f3f66c9` | Add mandatory Fan-out Subagents rule; add consolidated experiment record |
| `1cac37e` | Merge final A/B verdict into §12; language-agnostic split + CADL landing |
| `a16e2d9` | Fix E2 counts to re-verified grep output (74/2/72; run_aws 82; collect 7) |
| `debe662` | Add mandatory Token Efficiency Monitor gate |
| `bb30bb1` | Full English AGENTS.md (assembler) |
| `fff941b` | Finalize English AGENTS.md (no Chinese remains; anchors preserved) |

Experiment docs (also committed): `docs/superpowers/specs/2026-07-19-codegraph-ab-experiment-design.md`,
`docs/superpowers/plans/2026-07-19-codegraph-ab-experiment.md`,
`docs/superpowers/2026-07-19-codegraph-ab-experiment-record.md`.

## 3. Open / pending items

- **[IN FLIGHT — was being closed at handoff]** English AGENTS.md assembly + Token Efficiency Monitor
  audit. As of handoff: assembly committed (`fff941b`), Monitor (general-purpose-17)
  audit was launched and its verdict not yet seen in this session. A fresh session should
  check `git log -1` — if the latest AGENTS.md commit is `fff941b` or later and the
  file has no Chinese (`grep -nP`/`[一-鿿]` returns nothing), the task is effectively closed.
  If a Monitor refactor commit exists, adopt it.

- **[PENDING] Task #10 — level3-coverage Phase-2/3 PRODUCTS + collector expansion.**
  User chose "先扩 PRODUCTS+采集器" (expand PRODUCTS + collectors for EKS/CloudFront/
  Athena/RAM/SecretsManager) so the Phase-2/3 AIOps inference rules become live code
  (currently dead code because those services lack PRODUCTS entries). Needs its OWN spec+plan
  per dogfood discipline. `PRODUCTS` list lives in `aws-aiops-cruise/runbooks/scripts/_shared.py`.

- **[LOOSE END] Two untracked files in `git status`:**
  - `.codebuddy/` (session state — do NOT commit)
  - `docs/superpowers/specs/2026-07-19-skill-as-infrastructure-design.md` (input spec for an
    earlier plan; not part of this session's intended diff — leave or ask user before committing).

## 4. How a fresh session continues ("请帮我继续往下完成所有的任务")

1. Read this file (docs/RESUME-2026-07-19.md).
2. Confirm repo state: `git log --oneline -3`, `git status --short`, `wc -l AGENTS.md`,
   and verify AGENTS.md has no Chinese.
3. If the English-AGENTS.md Monitor task is not yet closed, wait for / re-run the Monitor
   verdict, then adopt its result.
4. Apply the §1 standing rules to whatever the user asks next. If the user's next request
   is open-ended ("continue all tasks"), prioritize Task #10 (spec+plan for PRODUCTS expansion),
   following Spec+Plan-before-implement + Fan-out + Token Efficiency Monitor + §12 split.
5. After closing work, update or delete this resume file.

## 5. Key repo facts (for context)

- Flat collection of AI-agent runbooks; each `aws-<svc>-ops/` is a SKILL, not source code.
- One meta-skill `aws-skill-generator/` is source of truth for structure/frontmatter/review.
- 34 `aws-*-ops` skills; `aws-aiops-orchestrator` uses `type: orchestrator-meta`
  (treated as composite-equivalent by the contract).
- GCL (Generator-Critic-Loop) is the adversarial quality gate (§11).
- CodeGraph = local tree-sitter code graph; indexes CODE symbols only, NOT Markdown.
