# AWS Skill Development Environment Variables
# Copy this file to .env and fill in your credentials
# DO NOT commit .env to version control!

# ========================================
# Environment Variable Location
# ========================================
# When working within this repo: place .env at project root
# When mixing with other Skills (Aliyun/JD Cloud/etc): place .env in working directory (cwd)
# Custom path: use --env-file parameter

# ========================================
# AWS API Credentials
# ========================================
# AWS RECOMMENDATION (in priority order):
#   1. IAM Role        - Use when running on EC2/Lambda/ECS/EKS (no keys needed)
#   2. AWS SSO         - Use for local dev: `aws configure sso` (see below)
#   3. STS AssumeRole  - Use for cross-account via temporary credentials
#   4. Access Keys     - LAST RESORT for local dev only; rotate every 90 days
#
# Standard AWS namespace prefix: AWS_*

# --- Base Credentials (always required when using explicit keys) ---
# Get from: AWS Console → IAM → Security credentials → Access keys
# https://console.aws.amazon.com/iam/home#/security_credentials
# Leave empty if using IAM Role, SSO, or ~/.aws/config profiles.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# --- Optional: Session Token (for STS temporary credentials) ---
# When set, the SDK/CLI treats the keys above as temporary credentials.
# Required when using IAM role assumed credentials or STS GetSessionToken.
# Leave empty when using long-term access keys.
AWS_SESSION_TOKEN=

# --- Optional: Named Profile (for SSO / AssumeRole / multi-account) ---
# Set to use a profile from ~/.aws/config instead of explicit keys above.
# Example: "dev", "prod", "sso-admin"
AWS_PROFILE=

# ========================================
# Optional: Default Region
# ========================================
# Default: us-east-1
# Options: us-east-1, us-east-2, us-west-1, us-west-2, eu-west-1, ap-northeast-1, etc.
# Full list: https://docs.aws.amazon.com/general/latest/gr/rande.html
AWS_DEFAULT_REGION=us-east-1

# ========================================
# Optional: CLI Configuration
# ========================================
# Output format for aws commands (json recommended for agents)
AWS_CLI_OUTPUT=json

# ========================================
# Optional: Development Settings
# ========================================
# Skill generation verbosity (true/false)
SKILL_GEN_VERBOSE=false

# ========================================
# Optional: Skill Config Rendering
# ========================================
# Used by skill assets/example-config.yaml to render {{env.*}} placeholders.
# Set these so generated config examples use your actual account/region.
AWS_ACCOUNT_ID=123456789012
# AWS_DEFAULT_VPC_ID=vpc-xxxxx
# AWS_DEFAULT_SUBNET_ID=subnet-xxxxx
# AWS_DEFAULT_INSTANCE_ID=i-xxxxx

# ========================================
# Priority Order (Agent Runtime)
# ========================================
# 1. Shell environment variables (highest priority)
# 2. .env file values (this file)
# 3. Default values (lowest priority)
#
# Example: If you set `export AWS_DEFAULT_REGION=eu-west-1` in shell,
# it will override the value in this file.

# ========================================
# SECURITY WARNINGS
# ========================================
# 1. NEVER commit .env files to version control (.gitignore blocks this)
# 2. Use separate .env files for different environments (dev/staging/prod)
# 3. Rotate credentials regularly (every 90 days recommended)
# 4. Grant minimal permissions to API keys (least privilege principle)
# 5. Never share .env files via email, chat, or any unsecured channel
# 6. Prefer IAM roles over long-term access keys when possible

# ========================================
# Multi-Cloud Mixing Example (Optional)
# ========================================
# When mixing AWS Skills with other cloud provider Skills,
# use namespace prefixes to avoid credential conflicts.
#
# Example .env for multi-cloud environment:
#
# # AWS - AWS_* prefix (standard)
# AWS_ACCESS_KEY_ID=your_aws_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret
# AWS_DEFAULT_REGION=us-east-1
#
# # JD Cloud - JDC_* prefix
# JDC_ACCESS_KEY=your_jdcloud_key
# JDC_SECRET_KEY=your_jdcloud_secret
# JDC_REGION=cn-north-1
#
# # Aliyun - ALIYUN_* prefix
# ALIYUN_ACCESS_KEY_ID=your_aliyun_key
# ALIYUN_ACCESS_KEY_SECRET=your_aliyun_secret
# ALIYUN_REGION=cn-hangzhou
#
# # Tencent Cloud - TENCENT_* prefix
# TENCENT_SECRET_ID=your_tencent_id
# TENCENT_SECRET_KEY=your_tencent_secret
# TENCENT_REGION=ap-guangzhou
#
# # Google Cloud Platform (GCP) - GCP_* prefix
# GCP_PROJECT_ID=your_gcp_project_id
# GCP_REGION=us-central1
# GCP_SERVICE_ACCOUNT_KEY_PATH=/path/to/service-account-key.json
# GCP_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
#
# # Microsoft Azure - AZURE_* prefix
# AZURE_SUBSCRIPTION_ID=your_subscription_id
# AZURE_TENANT_ID=your_tenant_id
# AZURE_CLIENT_ID=your_client_id
# AZURE_CLIENT_SECRET=your_client_secret
# AZURE_REGION=eastus
#
# # Huawei Cloud - HUAWEICLOUD_* prefix
# HUAWEICLOUD_ACCESS_KEY_ID=your_access_key_id
# HUAWEICLOUD_SECRET_ACCESS_KEY=your_secret_access_key
# HUAWEICLOUD_REGION=cn-north-4

# ========================================
# Credential Loading in Skills
# ========================================
# Agent loads credentials in the following order:
#   1. Check shell environment variables (export AWS_ACCESS_KEY_ID=xxx)
#   2. Check .env file in project root (this file)
#   3. Check ~/.aws/credentials and ~/.aws/config
#   4. Check IAM Role (EC2/Lambda/ECS instance metadata)
#
# The Agent MUST log each step:
#   [OK]   AWS_ACCESS_KEY_ID loaded from .env
#   [SKIP] AWS_ACCESS_KEY_ID not set (using AWS_PROFILE or IAM Role)
#   [FAIL] No valid credential source found
#
# Error messages MUST be specific:
#   - If AWS_ACCESS_KEY_ID is empty and AWS_PROFILE is empty and no IAM Role:
#     "AWS credentials not found. Please set one of the following:
#      1. AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY in .env
#      2. AWS_PROFILE for SSO / named profile
#      3. Attach IAM Role if running on AWS compute"
#   - If AWS_DEFAULT_REGION is empty:
#     "AWS_DEFAULT_REGION not set. Defaulting to us-east-1.
#      To change, set AWS_DEFAULT_REGION in .env or shell environment."
#   - If sts get-caller-identity fails:
#     "AWS credential verification failed: <exact error from AWS>
#      Common causes: invalid key, expired token, or insufficient permissions.
#      Run 'aws sts get-caller-identity' manually to diagnose."